# ThreeDLLM Environment Variables
# Copy this file to .env and fill in your values

# OpenAI API Key (required for VLM features)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Device selection for 3D generation
# Options: "auto" (default), "cuda" (for GPU), "cpu"
DEVICE=auto

# Optional: CUDA device selection (for GPU version)
# CUDA_VISIBLE_DEVICES=0
